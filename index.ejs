<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>

<div class="weather">
    <div class="current">
        <div class="info">
            <h2 class="city">Weather in <span id="cityName"></span>:</h2>
            <p class="temp">Temperature: <span id="temperature"></span> °F</p>
            <p class="humidity">Humidity: <span id="humidity"></span> %</p>
            <p class="wind">Wind Speed: <span id="windSpeed"></span> mph</p>
            <p class="description">Description: <span id="weatherDescription"></span></p>
        </div>
    </div>
    <div class="form-group">
        <form id="weatherForm" action="/" method="post">
            <input type="text" name="city" id="cityInput" placeholder="Enter city name" required>
            <button type="submit">Get Weather</button>
        </form>
    </div>
    <% if (error) { %>
        <div class="error">
            <p><%= error %></p>
        </div>
    <% } %>
</div>

<script src="/scripts.js"></script>
</body>
</html>
